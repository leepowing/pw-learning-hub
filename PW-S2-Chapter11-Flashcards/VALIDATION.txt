Validation completed
- TypeScript parse checks for all candidate source files.
- Isolated strict TypeScript check for new data, diagram, chapter page and
  changed FormulaFlashcards. Declarations stand in for existing external
  components, Next Link, KaTeX and studentStorage; not a full project build.
- Full supplied data registry: 45 new cards, 9 per section, globally unique
  IDs; every pre-existing card object remains unchanged.
- Cross-year selection and chapter helper include Chapter 11.
- Installer: app and src/app, read-only preflight, original-byte backups,
  idempotent installation, and conflict stops on each changed existing file.
- React + jsdom: select sections, disable empty selection, start 9-card
  session, flip before grading, correct/practice callback IDs, successful
  and unsuccessful save messages, return to selection. Storage and KaTeX
  are mocked in interaction tests; no live database requests were made.
- Five SVGs rendered from actual React code and visually reviewed.
- Mathematics content reviewed for exact roots, unit conversions,
  Pythagorean checks, fractions and surd simplifications.

Not verified here
- Full Next.js application build, whole-page browser layout and Vercel deploy.
- Live Supabase atomic save, actual retry delivery or cross-device sync.
  Existing storage implementation and review/progress pages are unchanged.
